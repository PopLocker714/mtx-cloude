# секреты — только локально / в Dokploy UI
.env

# локальные записи, если смонтируешь папкой вместо volume
recordings/

# зависимости (любой пакет)
node_modules/
**/node_modules/

# сборка веба
web/.output/
web/.nitro/
web/dist/
